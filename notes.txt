
Build the application:
  gradlew build

Build withour tests:
  gradlew build -x test

Run tests:
  gradlew clean test --info

Build and deploy to local Tomcat:
  gradlew clean war deploy
